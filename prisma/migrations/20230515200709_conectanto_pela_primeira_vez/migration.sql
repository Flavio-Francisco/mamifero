-- CreateTable
CREATE TABLE "Golfinho" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "species" TEXT NOT NULL,
    "characteristics" TEXT NOT NULL,
    "marine" BOOLEAN NOT NULL,

    CONSTRAINT "Golfinho_pkey" PRIMARY KEY ("id")
);
